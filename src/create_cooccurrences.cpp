#include <string>
#include <unordered_map>
#include <fstream>

// Create vocabulary dictionary

int main(int argc, char **argv) {
    if (argc != 4) {
        std::cerr << "USAGE: ./create_frequencies vocab.txt corpus.txt db_name\n";
    }
    
    std::string in_string;
    int freq;

    std::ifstream in_file()
    return EXIT_SUCCESS;
}