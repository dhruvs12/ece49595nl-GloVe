#pragma once
#include "glove_types.hpp"

constexpr idx_t iterations = @ITERATIONS@;
constexpr idx_t vector_size = @VECTOR_SIZE@;

constexpr idx_t x_max = @X_MAX@;
fp_t eta = @ETA@;
constexpr fp_t alpha = @ALPHA@;
fp_t gradient_clip = @GRADIENT_CLIP@;